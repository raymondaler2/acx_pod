import { createTheme } from "@mui/material/styles";

const BreakNotifUserTheme = createTheme({
  palette: {
    break: {
      main: "#F05524",
      contrastText: "white",
    },
  },
});

export default BreakNotifUserTheme;
